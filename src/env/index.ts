// Re-export server and client env for convenience
// Use server env in server components/API routes
// Use client env in client components
export { env as serverEnv } from "./server";
export { env as clientEnv } from "./client";
